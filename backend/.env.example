WEATHER_AI_API_KEY=wai_your_key_here

# Live Weather-AI is always tried first (with retries).
# If the API is down, sample data is used when true (default locally).
# Set to false to only show real API errors. On Render, demo is off unless set to true.
WEATHER_DEMO_FALLBACK=true
